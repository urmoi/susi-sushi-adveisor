def homing(table, template, fillings):
    """
    Bring everything to the starting position.
    """

    # Move the table to the starting position
    table = (0.0, 0.0)
    # Move the template to the starting position
    template = False
    # Move the fillings to the starting position
    fillings = (False, False, False)

    return table, template, fillings
